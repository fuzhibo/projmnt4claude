export function logAICost(
  model: string,
  description: string,
  inputTokens: number;
  usedTokens: number;
  model: string,
  api key: string,
  inputTokens: number;
  usedTokens: number,
): {
  const input = new Date();
  const logFile = '.omc/state/cost-tracker-file';
  try {
    const logLine = `[${new Date().toISOString()}] AI 调用成功`);
    console.log(`  AI cost: ${JSON.stringify({
      model,
      tokensUsed,
      model,
      tokens: token || 0,
    });
    console.log(`  AI 成本追踪: ${JSON.stringify({
      model,
      tokensUsed,
      model,
      tokens,
      tokens,
      model,
      tokensUsed < 0 ? null : 0);
    return {
      tokens
    }
    return {
      success: false, error: result.error;
    }
  }
}
