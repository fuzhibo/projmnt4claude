---
description: "Create tasks from natural language requirement descriptions - must complete the full CLI flow to persist tasks to .projmnt4claude directory"
argument-hint: "<description>"
---

# init-requirement - Natural Language Task Creation

Automatically analyze natural language descriptions and create structured tasks. No manual form-filling needed — one sentence converts a requirement into a task.

## Important: Task Persistence Requirement

**This command must complete the entire flow to ensure the task is persisted to `.projmnt4claude/tasks/`.**

After running this command:
1. CLI displays requirement analysis results (auto-extracts priority, role, complexity, checkpoints)
2. CLI asks for user confirmation (auto-confirmed in non-interactive mode)
3. CLI creates task files (meta.json and checkpoint.md)
4. CLI asks whether to add to execution plan (skipped in non-interactive mode)

## Prerequisites

Initialize the project before using this command:
```bash
projmnt4claude setup
```

## Usage

### Interactive Mode (default, for human users)
```bash
node ${CLAUDE_PLUGIN_ROOT}/skills/projmnt4claude/dist/projmnt4claude.js init-requirement "<requirement description>"
```
Interactive mode guides you through confirming analysis results, editing title/description/priority/role, and choosing whether to add to the execution plan.

### Non-Interactive Mode (recommended for AI agents)
```bash
# Use -y or --yes to skip all confirmations
node ${CLAUDE_PLUGIN_ROOT}/skills/projmnt4claude/dist/projmnt4claude.js init-requirement -y "<requirement description>"

# Also skip the "add to plan?" prompt
node ${CLAUDE_PLUGIN_ROOT}/skills/projmnt4claude/dist/projmnt4claude.js init-requirement -y --no-plan "<requirement description>"

# Skip checkpoint quality validation
node ${CLAUDE_PLUGIN_ROOT}/skills/projmnt4claude/dist/projmnt4claude.js init-requirement -y --skip-validation "<requirement description>"
```

## Options

| Option | Description |
|--------|-------------|
| `-y, --yes` | Non-interactive mode: skip all confirmations, create task directly with analysis results |
| `--no-plan` | Don't ask whether to add the task to the execution plan |
| `--skip-validation` | Skip checkpoint quality validation (not recommended) |

## Auto-Analysis Features

| Analysis Dimension | Description | Example Keywords |
|-------------------|-------------|-----------------|
| Priority | P0-P3 four levels | urgent→P0, important→P1, optional→P3 |
| Recommended Role | Match best execution role | UI→frontend, API→backend |
| Complexity | low/medium/high | refactor→high, fix→low |
| Checkpoints | Generated by task type | API task→design/implement/docs/test |
| Dependencies | Identify potential deps | login→depends on auth basics |

## Examples

### Create an API Task
```bash
node ${CLAUDE_PLUGIN_ROOT}/skills/projmnt4claude/dist/projmnt4claude.js init-requirement -y "Implement a user login API endpoint, high priority"
```
Auto-detected: Priority P1 (high), role backend, checkpoints include API design/implementation/docs/testing.

### Create a Frontend Task
```bash
node ${CLAUDE_PLUGIN_ROOT}/skills/projmnt4claude/dist/projmnt4claude.js init-requirement -y "Design and implement user profile page with avatar upload and info editing"
```
Auto-detected: Role frontend, checkpoints include UI prototype/components/interaction/responsive.

### Create an Urgent Fix Task
```bash
node ${CLAUDE_PLUGIN_ROOT}/skills/projmnt4claude/dist/projmnt4claude.js init-requirement -y "Urgent: fix production payment API timeout issue"
```
Auto-detected: Priority P0 (urgent), role backend.

### Create a Testing Task
```bash
node ${CLAUDE_PLUGIN_ROOT}/skills/projmnt4claude/dist/projmnt4claude.js init-requirement -y "Write unit tests and integration tests for the auth module"
```
Auto-detected: Role qa, checkpoints include unit tests/integration tests/coverage.

### Create a Documentation Task
```bash
node ${CLAUDE_PLUGIN_ROOT}/skills/projmnt4claude/dist/projmnt4claude.js init-requirement -y "Write usage documentation for the API module"
```
Auto-detected: Role writer, checkpoints include requirement collection/content writing/review.

### Create a Security Task
```bash
node ${CLAUDE_PLUGIN_ROOT}/skills/projmnt4claude/dist/projmnt4claude.js init-requirement -y "Fix XSS vulnerability in user input handling"
```
Auto-detected: Role security, complexity medium.

### Create a Database Task
```bash
node ${CLAUDE_PLUGIN_ROOT}/skills/projmnt4claude/dist/projmnt4claude.js init-requirement -y "Design and create database migration for user table"
```
Auto-detected: Checkpoints include data model/migration/data access layer.

## Keyword Recognition Reference

### Priority Keywords
- urgent/asap/立即/紧急 → P0
- important/high/优先/重要 → P1
- medium/default/中等 → P2
- optional/low priority/可选/低优先级 → P3

### Role Keywords
- UI/frontend/界面/前端 → frontend
- API/backend/后端/服务端 → backend
- test/qa/测试 → qa
- document/readme/文档 → writer
- security/漏洞/安全 → security
- performance/优化/性能 → performance
- architecture/设计/架构 → architect

### Complexity Keywords
- refactor/architecture/migrate/integration/系统 → high
- fix/update/add/modify/修复/添加 → low
- others → medium (default)

## Task Structure After Creation

```
.projmnt4claude/tasks/{TASK-ID}/
├── meta.json        # Task metadata (title, priority, role, checkpoints, etc.)
└── checkpoint.md    # Checkpoint checklist (markdown format)
```

Task ID format: `{type}-{priority}-{sequence}-{keyword}`, e.g. `bugfix-P0-001-fix-payment-timeout`.

## FAQ

**Q: What if I see "project not initialized"?**
A: Run `projmnt4claude setup` first to initialize the project management directory.

**Q: What if the analysis results are inaccurate?**
A: In interactive mode, you can manually edit each field. In non-interactive mode, use `task update` to modify afterwards.

**Q: How to view created tasks?**
A: Run `projmnt4claude task list` to see all tasks.

**Q: How to modify a created task's priority?**
A: Run `projmnt4claude task update <taskId> --priority P1`.
